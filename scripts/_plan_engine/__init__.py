"""_plan_engine — internal implementation package for plan_engine.py.

Imported via the thin shim scripts/plan_engine.py (Option A: the public name
stays a module file, not a package). Do not import this package directly from
consumers — use ``import plan_engine`` and the re-exported public surface.
"""
